## 2024-05-10 - Adding Comments is a Strict Constraint
**Learning:** Even if the performance optimization itself is technically sound and mathematically correct (like `React.memo` + `useCallback`), failing to add inline code comments as explicitly requested by user constraints ("Add comments explaining the optimization") will result in a lower review score ("Mostly Correct").
**Action:** Always double-check the explicit prompt boundaries and ensure all constraints (like adding descriptive comments) are implemented alongside the actual code optimization before submitting for code review.
